# ──────────────────────────────────────────────
# 仓库文件说明
#
# 【入库】—— 这些文件是博客的"源头"，换台机器或重建环境时必须靠它们还原：
#
#   _config.yml                Hexo 主配置（站点标题、URL、语言、时区、插件配置）
#   _config.butterfly.yml      Butterfly 主题配置（外观、功能开关、注入脚本等）
#   _config.landscape.yml      Hexo 默认备用主题配置，hexo init 自动生成，保留无害
#   package.json               声明项目依赖的插件版本，是 npm ci 的依据
#   package-lock.json          锁定所有依赖的精确版本，确保构建可复现
#   scaffolds/                 hexo new 命令的文章/页面模板，决定新文件的默认格式
#   source/_posts/             Markdown 文章，博客的核心内容
#   source/about/index.md      About 页面内容
#   source/css/i18n.css        自定义语言切换按钮样式
#   source/js/i18n.js          自定义中英语言切换逻辑
#   themes/.gitkeep            占位文件，保留 themes/ 目录结构（主题本身不入库）
#   .github/workflows/         GitHub Actions 工作流，控制自动构建和发布流程
#   .gitignore                 本文件自身也入库——它是项目级配置，确保任何人克隆后
#                              git 自动应用同一套忽略规则，换机器或多人协作时不会丢失
#   crontab.txt                服务器定时任务快照（crontab -l 的输出），记录博客自动迭代
#                              的调度配置，便于迁移到新机器时复现
#
# 【不入库】—— 以下文件由工具自动生成、体积过大、或属于本地私有状态，原因各见注释。
# ──────────────────────────────────────────────

# 操作系统产生的垃圾文件，与项目无关
.DS_Store
Thumbs.db

# Hexo 运行时数据库，记录文件修改时间用于增量构建，
# 每台机器各自生成，提交会引起无意义 diff
db.json

# 所有日志文件，运行时产生，内容因环境而异
*.log

# npm 依赖，体积庞大（数百 MB），通过 package.json 描述，
# 构建时由 npm ci 自动安装，无需纳入版本控制
node_modules/

# Hexo 构建产物（静态 HTML/CSS/JS），
# 由 GitHub Actions 自动生成并推送到 gh-pages 分支，
# 源码仓库只需保存源文件
public/

# hexo-deployer-git 的本地部署缓存目录，
# 本项目已改用 GitHub Actions 部署，该目录不会产生，保留为预防
.deploy*/

# 多配置合并文件，hexo 命令行临时生成，不属于源码
_multiconfig.yml

# Butterfly 主题本身是第三方仓库，
# 由 GitHub Actions 构建时 git clone 安装，
# 不提交进来避免仓库臃肿（主题约 50MB）
themes/butterfly/

# Hexo 构建缓存和临时文件，本地运行产生，与源码无关
.cache/
*.tmp

# VS Code 工作区配置，属于个人编辑器偏好，不应强加给其他贡献者
.vscode/

# Vim/其他编辑器的交换文件，编辑时产生，无内容价值
*.swp
*.swo

# Claude Code 的本地工作数据（计划文件、记忆文件、会话缓存），
# 属于 AI 辅助开发的本地状态，不属于博客源码
.claude/

# 系统 crontab 触发 Claude 迭代任务时写入的日志，
# 运行时产生，内容因机器和时间而异
cron.log
